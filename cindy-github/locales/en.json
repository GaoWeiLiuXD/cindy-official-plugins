{
  "name": "GitHub",
  "description": "Built-in GitHub service plugin. Add a Personal Access Token to read and write issues, pull requests, reviews, repositories, Actions, releases, search, and 117 other catalogued operations. Supports github.com only. The host encrypts the token and injects it only into GitHub API requests.",
  "whenToUse": "Use only for github.com repositories and links; use GitLab for GitLab instances. Supports issues, pull requests, reviews, merges, repositories, branches, commits, files, Actions, releases, search, artifacts, and logs. Configure a GitHub token in the plugin details first.",
  "tools": {
    "list_tools": {
      "description": "List this plugin's 117 GitHub operations across issues, pulls, repo, commits, actions, releases, search, users, orgs, events, notifications, gists, git_data, reactions, and meta. With no category, return category names, counts, and descriptions; with a category, return every operation in that category with its description and parameters."
    },
    "call_tool": {
      "description": "Run a GitHub operation returned by list_tools, passing args according to its parameter schema. Repository operations require explicit owner and repo values, which may be derived from git remote get-url origin when working in a repository. API-link fields are removed by default except html_url; set raw=true for the original JSON. Results over 50KB are written to the session work directory and only the file path is returned; out_file can set the path. Downloads such as Actions artifacts or log ZIPs require a destination directory in the top-level save_dir field of ghost_call."
    }
  }
}

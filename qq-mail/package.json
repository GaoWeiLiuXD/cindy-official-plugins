{
  "name": "@cindy-official/qq-mail",
  "private": true,
  "version": "0.2.0",
  "description": "Build-only dependencies for the Cindy QQ Mail plugin Node Worker.",
  "scripts": {
    "build": "esbuild src/entry.cjs --bundle --platform=node --format=cjs --target=node20 --outfile=node/worker.cjs --legal-comments=eof"
  },
  "dependencies": {
    "imapflow": "1.5.0",
    "mailparser": "3.9.14",
    "nodemailer": "9.0.3"
  },
  "devDependencies": {
    "esbuild": "0.27.0"
  }
}
